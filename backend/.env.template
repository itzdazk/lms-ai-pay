# Server Configuration
PORT=5000
NODE_ENV=development

# Database
# Thay đổi username, password, host, port theo cấu hình PostgreSQL của bạn
DATABASE_URL="postgresql://username:password@localhost:5432/lms_ai_pay?schema=public"

# JWT
JWT_SECRET=47322bc29a646fc688cf0b46a5fa045267bd8b53b534c07feda7cfb8a71ab5ce
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=6619fe8e5d01e4f481fb9b12230eba5c80f8a73622bf9cca0e4f801d9e9f6976
JWT_REFRESH_EXPIRES_IN=30d

# Email Configuration (Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=mr.blackk2502@gmail.com
SMTP_PASSWORD=ixzx egxn ntmr mbqn
EMAIL_FROM=noreply@lmsaipay.com
EMAIL_FROM_NAME=LMS AI Pay

# Frontend URLS
CLIENT_URL=http://localhost:3000
EMAIL_VERIFICATION_URL=http://localhost:3000/verify-email
PASSWORD_RESET_URL=http://localhost:3000/reset-password

# OpenAI API
OPENAI_API_KEY=your-openai-api-key

# Payment Gateways
VNPAY_TMN_CODE=your-vnpay-tmn-code
VNPAY_HASH_SECRET=your-vnpay-hash-secret
VNPAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNPAY_RETURN_URL=http://localhost:5000/api/payments/vnpay/callback

MOMO_PARTNER_CODE=your-momo-partner-code
MOMO_ACCESS_KEY=your-momo-access-key
MOMO_SECRET_KEY=your-momo-secret-key
MOMO_API_URL=https://test-payment.momo.vn/v2/gateway/api/create
MOMO_RETURN_URL=http://localhost:5000/api/payments/momo/callback

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=104857600
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp
ALLOWED_VIDEO_TYPES=mp4,webm,mov

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Redis / BullMQ
REDIS_HOST=localhost
REDIS_PORT=6379
# Leave empty if no password is set on your Redis instance
REDIS_PASSWORD=
# Set to true only when connecting to a TLS-enabled Redis (e.g., cloud providers)
REDIS_TLS=false

# Video progress
# Set the fraction of the video that must be watched to count as completed (e.g., 0.7 = 70%)
VIDEO_COMPLETE_THRESHOLD=0.7

